# Public read API (deploy app-analytics-read first).
# When set, the viewer calls this URL and does not need local wrangler auth.
VITE_READ_API_URL=https://app-analytics-read.my-app-logs.workers.dev

# Leave unset for local dev. GitHub Actions sets /smarty/ for Pages builds.
# VITE_BASE=/smarty/
